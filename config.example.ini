[archive]
# Workspace root for your Markdown notes / Markdown 工作区根目录
workspace = /Users/you/your-notebook

# Tag navigation directory, relative to workspace / 标签导航目录，相对 workspace
tags_dir = .tags

# Durable archive copy directory, relative to workspace / 内容存档目录，相对 workspace
archive_dir = .archive
